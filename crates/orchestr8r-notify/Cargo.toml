[package]
name = "orchestr8r-notify"
version = "0.1.0"
edition = "2021"

[dependencies]
async-trait = "0.1"
thiserror = "1"
notify-rust = "4"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt"] }

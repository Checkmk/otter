# Add a /healthz endpoint

Add an HTTP endpoint at `/healthz` that returns 200 with body `ok`.
Wire it into the existing router and add a test.

# (DRAFT) Rework the config loader

Not ready to hand off yet — still deciding on the approach.
otter skips `(DRAFT)` tasks until you remove the marker.
